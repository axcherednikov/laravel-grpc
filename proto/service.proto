syntax = "proto3";

package app.grpc.gen.service;

service Echo {
    rpc Ping (Request) returns (Response) {}
}

message Request {
    string msg = 1;
    int32 number = 2;
}

message Response {
    string test = 1;
}

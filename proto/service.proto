syntax = "proto3";

package grpc.service.test;

service Test {
    rpc Ping (Request) returns (Response) {}
}

message Request {
    string msg = 1;
    int32 number = 2;
}

message Response {
    string test = 1;
}
